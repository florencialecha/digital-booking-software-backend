stages:
  - deploy

deploy:
  stage: deploy
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y openssh-client
  script:
    - ssh-keyscan <3.131.138.206> >> ~/.ssh/known_hosts
    - ssh -i /home/ubuntu/.ssh/gitlab-runner ubuntu@<3.131.138.206> "echo 'Hello from GitLab CI/CD!'"
