#!/bin/bash

# Set variables
HOST="3.131.138.206"
USER="ubuntu"
SSH_KEY_PATH="6KeyPair"

# Connect to EC2 instance and execute command
ssh -i "$SSH_KEY_PATH" "$USER"@"$HOST" "echo 'Hello from the EC2 instance!'"
