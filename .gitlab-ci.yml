stages:
  - deploy

deploy:
  stage: deploy
  variables:
    SSH_PRIVATE_KEY: $6KeyPair
  before_script:
    
  script:
    - ssh ubuntu@<3.131.138.206> "echo 'Hello from GitLab CI/CD!'"
  tags:
    - ApiBack
