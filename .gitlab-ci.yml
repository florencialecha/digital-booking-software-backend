stages:
  - deploy

deploy:
  stage: deploy
  variables:
    SSH_PRIVATE_KEY: $6KeyPair
  script:
    - echo "Hello"
    - sudo ssh -i ${SSH_PRIVATE_KEY} ubuntu@3.131.138.206 "echo 'Hello from EC2 instance'"
