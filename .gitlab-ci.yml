

stages:
  - buildAndDeployBack

build:
  stage: buildAndDeployBack
  script:
    - cd back
    - sudo rm -r /out/artifacts/back_jar 

    - mvn clean package

    - systemctl restart ApiBack

  tags:
    - back

